import React from 'react';

import { ResultWindow } from './components/ResultWindow';

// 出力結果ウインドウを実行するためのアプリケーション コンポーネントを作成します。
const ResultApp = () => {
  return <ResultWindow />;
};

export default ResultApp;
